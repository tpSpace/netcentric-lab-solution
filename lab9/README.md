# Please create .env before run the program

## .env file should contain the following variables
## In this case I use supabase as the database.

```bash
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
```